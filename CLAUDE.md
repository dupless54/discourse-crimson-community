@AGENTS.md

# Claude Code adapter
Use nearest local `AGENTS.md` only for the touched presence/profile/schema surface. Read a matching `.agents/skills/*/SKILL.md` only on demand. Source-verify current Discourse PresenceChannel/serializer behavior rather than relying on memory.
