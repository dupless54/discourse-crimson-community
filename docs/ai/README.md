# AI context system
Keep resumable facts only. `CURRENT_STATE.md` is the short checkpoint; `WORKFLOW.md` owns cross-model review roles. For multi-session features use only `docs/ai/work/<feature>/state.md`, `progress.md`, and `implementation-plan.md` unless more detail is truly necessary.
